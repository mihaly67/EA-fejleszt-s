/**
 * Defines action types used for communication between extension components.
 * These constants are used to identify actions throughout the extension.
 */
export const ACTIONS: {
  /**
   * Action to toggle the visibility of the debug overlay.
   */
  TOGGLE_DEBUG_OVERLAY: string;
} = {
  TOGGLE_DEBUG_OVERLAY: "toggle-debug-overlay",
};

export default ACTIONS;
